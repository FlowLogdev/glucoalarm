# Server-only — used by the Next.js API routes (login, logout, proxy) to
# reach the Worker. Never exposed to the browser (no NEXT_PUBLIC_ prefix);
# the browser only ever talks to this Next.js app's own /api/* routes.
API_BASE_URL=http://localhost:8787
